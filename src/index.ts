// create-react-app entrypoint
//
// It looks like CRA _has_ to look at src/index as its root.
// I'd recommend putting a very thin file here with a comment (like this one)
// so that all your front-end code can still live in "frontent/"
import './frontend'
